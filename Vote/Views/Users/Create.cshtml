@{
    Layout = "_Layout";
  }
  
  @using Vote.Models
  @model Vote.Models.User
  
  @using (Html.BeginForm())
  {
    @Html.LabelFor(model => model.Name)
    @Html.TextBoxFor(model => model.Name)
  
    <input type="submit" value="Add new User" class="btn btn-default" />
  }
  
  