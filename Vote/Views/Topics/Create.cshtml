@{
  Layout = "_Layout";
}

@using Vote.Models
@model Vote.Models.Topic

@using (Html.BeginForm())
{
  @Html.LabelFor(model => model.Name)
  @Html.TextBoxFor(model => model.Name)

  <input type="submit" value="add new topic" class="btn btn-default" />
}

